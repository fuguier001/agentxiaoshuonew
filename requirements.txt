# ==========================================
# 多 Agent 协作小说系统 - Python 依赖
# ==========================================

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 必需依赖
python-dotenv>=1.0.0

# 重试机制
tenacity>=8.2.0

# 异步任务队列
celery>=5.4.0
redis>=5.0.0

# HTTP 客户端
httpx>=0.28.0

# 向量数据库
chromadb>=0.4.0
# 或者使用 FAISS
# faiss-cpu>=1.7.4

# 知识图谱
networkx>=3.2.0
# 或者使用 Neo4j (可选)
# neo4j>=5.0.0

# 数据库
sqlite-utils>=3.35
aiosqlite>=0.19.0

# Git 集成
gitpython>=3.1.40

# 工具函数
pyyaml>=6.0.1
python-jose[cryptography]>=3.3.0  # JWT 支持 (可选)

# 日志
loguru>=0.7.2

# 测试 (可选)
pytest>=8.3.0
pytest-asyncio>=0.24.0

# 开发工具 (可选)
black>=23.12.0
flake8>=7.0.0
mypy>=1.8.0

# 导出功能 (可选)
ebooklib>=0.18  # EPUB 导出
